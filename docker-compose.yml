services:
  privet:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      API_BASE_URL: "${API_BASE_URL:-https://whisper-skynet.bourbaki-lab.duckdns.org}"
